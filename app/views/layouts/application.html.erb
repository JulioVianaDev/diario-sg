<!DOCTYPE html>
<html>
  <head>
    <title>Diario</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

  <nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if current_user %>
          <div class="div-central-navbar">
            <div class="links"/>
              <%= link_to "Home",root_path ,class: "navbar-brand"%>
              <%= link_to "Posts",posts_path ,class: "navbar-brand"%>
              <%= link_to "Galeria",galleries_path ,class: "navbar-brand"%>
            </div>
            <div class="botoes">
              <li class="nav-item">
                <%= link_to "Sair", destroy_user_session_path, 'data-turbo-method': :delete ,class:"btn btn-danger" %>
              </li>
            </div>
          </div>
        <% else %>
          <li class="nav-item">
            <%= link_to "Cadastrar", new_user_registration_path, class: "btn btn-info"%>
    
          </li>
          <li class="nav-item">
            <%= link_to "Logar", new_user_session_path, class: "btn btn-success" %>
          </li>
        <% end %>
        </div>
      </ul>
    </div>
  </div>
</nav>
    <%= yield %>
  </body>
</html>
